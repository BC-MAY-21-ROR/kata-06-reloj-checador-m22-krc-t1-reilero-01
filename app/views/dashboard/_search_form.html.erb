    <%= form_with url: search_dashboard_index_path, method: :post, data: {controller: "debounce", debounce_target: "form" } do |form|  %>
      <%= form.search_field :name_search, data: { action: "input->debounce#search" } %>
    <% end %>
    <div id="search_results"></div>
    